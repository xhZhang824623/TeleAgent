# HTTPS 证书目录（无域名 / 无证书时）

- **不放入任何文件**：运行 `./start-dev.sh` 时会自动在此目录生成**自签名证书**（`cert.pem`、`key.pem`），用于 HTTPS（端口 9443）。
- 浏览器访问会提示「不安全」，属正常现象，可继续访问。
- 有正式域名和证书时：将你的 `cert.pem`、`key.pem`（或对应文件）放到此目录即可，nginx 会使用它们。
